diff -Naur /home/stack/templates/compute.yaml /home/stack/templates-net/compute.yaml
--- /home/stack/templates/compute.yaml	2016-07-27 15:38:05.482477887 -0400
+++ /home/stack/templates-net/compute.yaml	2016-07-27 15:39:44.490794267 -0400
@@ -323,6 +323,11 @@
     type: OS::TripleO::Compute::Ports::StoragePort
     properties:
       ControlPlaneIP: {get_attr: [NovaCompute, networks, ctlplane, 0]}
+      
+  StorageMgmtPort:
+    type: OS::TripleO::Compute::Ports::StorageMgmtPort
+    properties:
+      ControlPlaneIP: {get_attr: [NovaCompute, networks, ctlplane, 0]}
 
   TenantPort:
     type: OS::TripleO::Compute::Ports::TenantPort
@@ -335,6 +340,7 @@
       ControlPlaneIp: {get_attr: [NovaCompute, networks, ctlplane, 0]}
       InternalApiIpSubnet: {get_attr: [InternalApiPort, ip_subnet]}
       StorageIpSubnet: {get_attr: [StoragePort, ip_subnet]}
+      StorageMgmtIpSubnet: {get_attr: [StorageMgmtPort, ip_subnet]}
       TenantIpSubnet: {get_attr: [TenantPort, ip_subnet]}
 
   NetworkDeployment:
@@ -533,6 +539,9 @@
   storage_ip_address:
     description: IP address of the server in the storage network
     value: {get_attr: [StoragePort, ip_address]}
+  storage_mgmt_ip_address:
+    description: IP address of the server in the storage_mgmt network
+    value: {get_attr: [StorageMgmtPort, ip_address]}
   tenant_ip_address:
     description: IP address of the server in the tenant network
     value: {get_attr: [TenantPort, ip_address]}
diff -Naur /home/stack/templates/environments/network-isolation.yaml /home/stack/templates-net/environments/network-isolation.yaml
--- /home/stack/templates/environments/network-isolation.yaml	2016-07-27 15:38:05.483477890 -0400
+++ /home/stack/templates-net/environments/network-isolation.yaml	2016-07-27 15:41:45.894182207 -0400
@@ -27,7 +27,8 @@
   OS::TripleO::Compute::Ports::InternalApiPort: ../network/ports/internal_api.yaml
   OS::TripleO::Compute::Ports::StoragePort: ../network/ports/storage.yaml
   OS::TripleO::Compute::Ports::TenantPort: ../network/ports/tenant.yaml
-
+  OS::TripleO::Compute::Ports::StorageMgmtPort: ../network/ports/storage_mgmt.yaml
+  
   # Port assignments for the ceph storage role
   OS::TripleO::CephStorage::Ports::StoragePort: ../network/ports/storage.yaml
   OS::TripleO::CephStorage::Ports::StorageMgmtPort: ../network/ports/storage_mgmt.yaml
diff -Naur /home/stack/templates/overcloud-resource-registry-puppet.yaml /home/stack/templates-net/overcloud-resource-registry-puppet.yaml
--- /home/stack/templates/overcloud-resource-registry-puppet.yaml	2016-07-27 15:38:05.489477909 -0400
+++ /home/stack/templates-net/overcloud-resource-registry-puppet.yaml	2016-07-27 15:42:40.205355756 -0400
@@ -87,7 +87,8 @@
   OS::TripleO::Compute::Ports::InternalApiPort: network/ports/noop.yaml
   OS::TripleO::Compute::Ports::StoragePort: network/ports/noop.yaml
   OS::TripleO::Compute::Ports::TenantPort: network/ports/noop.yaml
-
+  OS::TripleO::Compute::Ports::StorageMgmtPort: network/ports/noop.yaml
+  
   # Port assignments for the ceph storage role
   OS::TripleO::CephStorage::Ports::StoragePort: network/ports/noop.yaml
   OS::TripleO::CephStorage::Ports::StorageMgmtPort: network/ports/noop.yaml
diff -Naur /home/stack/templates/puppet/compute-puppet.yaml /home/stack/templates-net/puppet/compute-puppet.yaml
--- /home/stack/templates/puppet/compute-puppet.yaml	2016-07-27 15:38:05.489477909 -0400
+++ /home/stack/templates-net/puppet/compute-puppet.yaml	2016-07-27 15:39:15.368701207 -0400
@@ -325,6 +325,11 @@
     properties:
       ControlPlaneIP: {get_attr: [NovaCompute, networks, ctlplane, 0]}
 
+  StorageMgmtPort:
+    type: OS::TripleO::Compute::Ports::StorageMgmtPort
+    properties:
+      ControlPlaneIP: {get_attr: [NovaCompute, networks, ctlplane, 0]}
+      
   TenantPort:
     type: OS::TripleO::Compute::Ports::TenantPort
     properties:
@@ -338,6 +343,8 @@
       InternalApiIpUri: {get_attr: [InternalApiPort, ip_address_uri]}
       StorageIp: {get_attr: [StoragePort, ip_address]}
       StorageIpUri: {get_attr: [StoragePort, ip_address_uri]}
+      StorageMgmtIp: {get_attr: [StorageMgmtPort, ip_address]}
+      StorageMgmtIpUri: {get_attr: [StorageMgmtPort, ip_address_uri]}
       TenantIp: {get_attr: [TenantPort, ip_address]}
       TenantIpUri: {get_attr: [TenantPort, ip_address_uri]}
 
@@ -347,6 +354,7 @@
       ControlPlaneIp: {get_attr: [NovaCompute, networks, ctlplane, 0]}
       InternalApiIpSubnet: {get_attr: [InternalApiPort, ip_subnet]}
       StorageIpSubnet: {get_attr: [StoragePort, ip_subnet]}
+      StorageMgmtIpSubnet: {get_attr: [StorageMgmtPort, ip_subnet]}
       TenantIpSubnet: {get_attr: [TenantPort, ip_subnet]}
 
   NetworkDeployment:
@@ -609,6 +617,9 @@
   storage_ip_address:
     description: IP address of the server in the storage network
     value: {get_attr: [StoragePort, ip_address]}
+  storage_mgmt_ip_address:
+    description: IP address of the server in the storage_mgmt network
+    value: {get_attr: [StorageMgmtPort, ip_address]}
   tenant_ip_address:
     description: IP address of the server in the tenant network
     value: {get_attr: [TenantPort, ip_address]}
